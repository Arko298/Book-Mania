export interface Book {
    id: string
    title: string
    author: string
    cover: string
    rating: number
    ratingCount: number
    description: string
    pageLength: number
    language: string
    publisher: string
    publishingDate: string
  }
export interface RecommendedBook {
    id: string
    title: string
    author: string
    cover: string
    rating: number
    ratingCount: number
    description: string
  }
